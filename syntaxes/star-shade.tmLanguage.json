{
	"$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
	"name": "StarShade",
	"patterns": [
		{ "include": "#comments" },
		{ "include": "#strings" },
		{ "include": "#numbers" },
		{ "include": "#keywords" },
		{ "include": "#types" },
		{ "include": "#functions" },
		{ "include": "#decorators" },
		{ "include": "#constants" },
		{ "include": "#operators" },
		{ "include": "#variables" }
	],
	"repository": {
		"comments": {
			"patterns": [
				{ "name": "comment.line.double-dollar.starshade", "match": "\\$\\$.*$" }	
			]
		},
		"strings": {
			"patterns": [
				{ "name": "string.quoted.double.starshade", "begin": "\"", "end": "\"", "captures": { "0": { "name": "string.quoted.double.starshade" } } },
				{ "name": "string.quoted.single.starshade", "begin": "'", "end": "'", "captures": { "0": { "name": "string.quoted.single.starshade" } } }
			]
		},
		"numbers": {
			"patterns": [
				{ "name": "constant.numeric.float.starshade", "match": "\\b\\d+\\.\\d+\\b" },
				{ "name": "constant.numeric.integer.starshade", "match": "\\b\\d+\\b" },
				{ "name": "constant.numeric.property_definition.starshade", "match": "\\b\\d+\\D\\b" }
			]
		},
		"keywords": {
			"patterns": [
				{ "name": "keyword.control.starshade", "match": "\\b(fn|return|if|else|elif|for|while|in|break|continue|struct)\\b" }	
			]
		},
		"types": {
			"patterns": [
				{ "name": "storage.type.starshade", "match": "\\b(Vector2|Vector3|Vector4|Color|Matrix4x4|float|int|Texture2D|SamplerState|float3|float4)\\b" }	
			]
		},
		"functions": {
			"patterns": [
				{ "name": "entity.name.function.starshade", "match": "\\b[a-zA-Z_][a-zA-Z0-9_]*(?=\\()" }	
			]
		},
		"decorators": {
			"patterns": [
				{ "name": "keyword.control.annotation.starshade", "match": "@[a-zA-Z_][a-zA-Z0-9_]*(?=\\s*\\()" }	
			]
		},
		"constants": {
			"patterns": [
				{ "name": "constant.language.starshade", "match": "\\b(true|false|null)\\b" }	
			]
		},
		"operators": {
			"patterns": [
				{ "name": "keyword.operator.starshade", "match": "[+\\-*/%=<>!&|^~]+" }	
			]
		},
		"variables": {
			"patterns": [
				{ "name": "variable.constant.starshade", "match": "\\b[a-zA-Z_][a-zA-Z0-9_]*\\b" }
			]
		}
	},
	"scopeName": "source.star"
}